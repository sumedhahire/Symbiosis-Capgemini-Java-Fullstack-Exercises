create procedure to access table


CREATE or REPLACE procedure changetable(v_roll NUMBER)
IS
--v_name is type of student.name
   v_name student.name%TYPE;
BEGIN
	SELECT name into v_name from student where roll = v_roll;
	
	dbms_output.put_line(v_name);
END;
/


run this procedure:
	1)@"path";
	
	2)>declare
		input NUMBER:=3
	   BEGIN	
		 changetable(input);
	   END;
	sumedh
   
   
   